// Bump this to invalidate every visitor's saved unlock (cookie + localStorage).
// v1 -> original launch · v2 -> reset on 2026-08-18 (pre-relaunch with new lineup)
export const UNLOCK_VERSION = "2";
